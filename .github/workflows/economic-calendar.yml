name: Economic Calendar Bot

on:
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Test mode'
        required: false
        type: choice
        options:
          - 'none'
          - 'tomorrow'
          - 'weekly'
        default: 'none'
  schedule:
    - cron: '0 8 * * 1'  # Monday 8 AM UTC
    - cron: '0 8 * * *'  # Daily 8 AM UTC
    - cron: '0 9-17/2 * * 1-5'  # Every 2 hours during market hours

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 pytz
      
      - name: Run bot
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          if [ "${{ github.event.inputs.test_mode }}" == "tomorrow" ]; then
            echo "🧪 Testing tomorrow's events..."
            python bot.py --tomorrow
          elif [ "${{ github.event.inputs.test_mode }}" == "weekly" ]; then
            echo "📅 Testing weekly preview..."
            python bot.py --weekly
          else
            echo "⏰ Running normal bot logic..."
            python bot.py
          fi
        
  opex_2026:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: pip install requests
      
      - name: Run OPEX 2026 bot
        env:
          OPEX_WEBHOOK: ${{ secrets.OPEX_WEBHOOK }}
        run: python opex_2026_bot.py
